# Public build-time API origin. Production must be HTTPS canonical — never localhost or :8390.
# Leave unset until Pixel Quest API has evidenced production URL.
# VITE_API_BASE=https://example.invalid
